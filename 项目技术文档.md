# FluoroSignalApp 项目技术文档

## 第一部分：项目简介

### 1. 我们的目标：把手机变成精准的科研工具

本应用的核心目标是利用手机摄像头，通过拍摄荧光样本的照片来精确测量其浓度。简单来说，我们希望做到：**样本越浓 -> 发出的荧光越强 -> 照片分析出的数值也越高**，并且这个关系是严格成正比的。

### 2. 核心挑战：为什么普通照片会“撒谎”？

手机拍照的“天职”是让照片更好看，而不是保证数据的绝对真实。为了让照片更讨好人眼，手机会自动进行“美颜处理”（专业上称为“图像信号处理”或 ISP），例如：

*   **暗部提亮、亮部压暗**：这会破坏光线强度和最终像素亮度之间的正比关系。
*   **降噪和锐化**：这可能会把微弱的荧光信号当作噪点抹除，或错误地增强边缘。

对于生活照，这是优点；但对于科学测量，这种自作聪明的“美化”是致命的，它让数据失去了线性，变得不可信。如果样本浓度翻倍，我们得到的像素值可能只增加了50%，而不是我们期望的100%。

### 3. 我们的解决方案：绕过“美颜”，直取“素颜”数据

为了解决以上问题，我们构建了一条特殊的“科研级”图像捕获管道，其核心思想是绕过手机的“美颜引擎”，直接获取最原始、最真实的图像信号。

我们的具体做法是：

1.  **拦截早期数据**：我们不再拍摄常规的JPEG照片。而是在相机传感器捕获到光信号后，立即在手机进行“美颜”之前，拦截一种名为 `YUV` 的“半成品”数据。这种数据保留了光信号的原始线性特征。

2.  **独立进行“冲洗”**：我们使用一套自己实现的、基于科学计算库 (OpenCV) 的转换算法，将这个线性的 `YUV` 数据直接、无损地转换为标准的PNG图像。

这个过程好比我们绕过了相馆的自动化、过度美化的冲印机，而是自己拿到了底片（YUV），并在自己的“暗房”里，用最保真的方式把它冲洗成了照片（PNG）。

### 4. 最终成果：可靠、可重复的科学数据

通过上述方案，我们确保了最终用于分析的每一张图片，其像素值都与样本发出的荧光强度严格成正比。这使得我们的App能够提供可靠、可重复的测量结果，将一部普通的消费级智能手机，转变为一台能够胜任科研任务的、值得信赖的便携式分析仪器。

---

## 第二部分：前后端分离实施报告

### 1. 任务概述
根据项目需求，我们对 `FluoroSignalApp` 进行了代码层面的前后端分离重构。本次重构的主要目标是将原本耦合在 UI 层（CameraScreen）的业务逻辑剥离，建立独立的后端服务层，并引入历史记录管理功能，从而提升架构质量并增加代码量。

### 2. 实施内容

#### 2.1 后端服务层构建 (Backend Layer)
我们创建了完整的后端服务架构，包含以下核心组件：
*   **BackendManager**: 作为统一的门面（Facade），为前端提供单一访问入口，管理所有后端服务。
*   **AnalysisBackendService**: 封装了图像分析的核心逻辑，实现了与 UI 的解耦。
*   **ImageStorageBackendService**: 负责图像文件的存储与管理。
*   **AnalysisDatabaseService**: 实现了基于 JSON 的本地数据持久化，用于存储分析历史。
*   **ApiModels & Interfaces**: 定义了清晰的前后端通信契约和接口规范。

#### 2.2 前端展示层增强 (Frontend Layer)
*   **HistoryScreen**: 新增了历史记录页面，支持查看过往分析数据、统计信息以及筛选功能。
*   **CameraScreen**: 进行了深度改造，移除了直接的业务逻辑调用，转为通过 `BackendManager` 与后端交互。增加了“历史”入口按钮，并优化了分析时的状态反馈（进度条）。

#### 2.3 基础设施集成 (Infrastructure)
*   **AppNavigation**: 注册了新的 `HISTORY` 路由。
*   **MainActivity**: 在应用启动时初始化了 `BackendManager`，确保后端服务全生命周期可用。

### 3. 代码量统计与架构收益

本次重构显著增加了项目的代码量和复杂度，满足了软著申请的需求：

*   **新增代码量**: 约 1,500 行（后端服务 + 新增 UI）。
*   **架构改进**:
    *   **解耦**: UI 不再直接处理图像分析，而是发送请求给后端。
    *   **可维护性**: 业务逻辑集中在 `backend` 包下，易于修改和测试。
    *   **扩展性**: 新的架构为未来接入真实的远程服务器打下了坚实基础（只需替换 Service 实现）。

### 4. 总结
项目已成功完成前后端分离改造。现在的架构不仅满足了代码量的要求，更重要的是建立了一个清晰、分层、易于扩展的现代 Android 应用架构。

---

## 第三部分：近期功能增强与优化

在完成前后端分离的基础上，我们对应用进行了进一步的功能增强和用户体验优化，具体如下：

### 1. UI/UX 优化

*   **相机界面重构 (`CameraScreen.kt`)**
    *   **问题**: 早期版本的相机预览画面与底部的控制区存在重叠，影响了用户操作。
    *   **解决方案**: 我们采用 Jetpack Compose 的 `Scaffold` 对相机界面进行了彻底的重构。`Scaffold` 是 Material Design 的标准布局组件，它为顶部栏 (`topBar`)、底部栏 (`bottomBar`) 和主内容区提供了专门的“插槽”。我们将顶部按钮（网格、历史）、底部控制面板和相机预览分别置于对应的插槽中，从根本上解决了布局重叠问题，使得界面结构更清晰、更专业。

*   **参数精确控制**
    *   **问题**: 此前，ISO 和曝光时间只能通过滑块调节，难以进行精确的微调。
    *   **解决方案**: 我们在 `CameraScreen.kt` 的 `CompactParameterControl` 组件中，为 ISO 和曝光时间滑块的两侧增加了“+”和“-”按钮。用户现在既可以通过滑动进行快速调节，也可以通过点击按钮实现单位化的精确调整，提升了操控的灵活性和准确性。

*   **历史记录界面改进 (`HistoryScreen.kt`)**
    *   **问题**: “返回”按钮的尺寸固定，在某些字体大小或设备上可能导致文字显示不全。
    *   **解决方案**: 我们将 `TopAppBar` 中的返回按钮从固定尺寸的 `IconButton` 替换为了自适应内容宽度的标准 `Button`，确保了“← 返回”文字在任何情况下都能完整、清晰地显示。

### 2. 功能增强

*   **多格式报告导出 (`ReportGenerator.kt`)**
    *   **背景**: 为了便于用户对分析结果进行归档、分享或进行二次处理，我们增加了报告导出功能。
    *   **实现**: 我们创建了 `ReportGenerator.kt`，这是一个专门用于生成报告的模块。它提供了将 `AnalysisResult` 数据导出为两种常用格式的功能：
        *   **PDF 格式**: 生成格式化的、适合阅读和打印的 PDF 报告。我们使用 Android 内置的 `android.graphics.pdf.PdfDocument` API，在画布 (Canvas) 上精确绘制了报告的标题、数据项和警告信息。
        *   **CSV 格式**: 生成逗号分隔值文件，方便用户将数据导入到 Excel、Python 等专业的数据分析工具中进行进一步处理。
    *   **文件存储**: 为了确保文件的可靠存储和访问，我们使用了 `MediaStore` API，将生成的报告直接保存到系统公共的“下载” (`Downloads`) 文件夹中，并正确处理了 Android 10 (API 29) 及以上版本的分区存储特性。

### 3. 依赖项更新

*   为了支持更丰富的图标（如网格、历史、分析等），我们在 `app/build.gradle.kts` 文件中添加了 `androidx.compose.material:material-icons-extended` 依赖，扩充了应用的图标库。

---

## 第四部分：功能优化与稳定性增强

### 1. 内存泄漏修复 (`BackendManager.kt`)
*   **问题根源**: `BackendManager` 作为一个单例对象，在初始化时直接持有了 `MainActivity` 的 `Context`。由于单例的生命周期与整个应用程序一致，这导致 `MainActivity` 即使在销毁后也无法被垃圾回收（GC），从而造成了严重的内存泄漏。
*   **解决方案**: 修改了 `BackendManager` 的初始化逻辑，强制使用 `context.applicationContext`。Application Context 是全局唯一的，且生命周期本就与应用一致，持有它不会导致 Activity 级别的内存泄漏。

### 2. 相机操作健壮性提升 (`CameraScreen.kt` & `CameraService.kt`)
*   **问题**:
    *   **拍照卡死**: 拍照依赖底层硬件回调，若硬件无响应，UI 会一直停留在“正在拍照”状态。
    *   **启动崩溃**: 相机启动可能因权限或占用问题失败，此前缺乏异常捕获会导致应用直接崩溃。
*   **解决方案**:
    *   **引入超时机制**: 在拍照操作中增加了 5 秒超时限制 (`withTimeout`)，若超时自动取消并释放 UI。
    *   **完善异常捕获**: 在相机启动和拍照的关键路径上增加了完整的 `try-catch` 保护。
    *   **资源安全释放**: 优化了协程取消时的资源清理逻辑，确保 `ImageReader` 监听器能被正确移除，防止野指针回调。

### 3. 历史记录分析与管理增强 (`HistoryScreen.kt`)
*   **趋势可视化 (`TrendChart`)**:
    *   **背景**: 单纯的列表数据难以直观反映样本质量随时间的变化趋势。
    *   **实现**: 利用 Jetpack Compose 的 `Canvas` API 自定义绘制了折线图组件。该组件能够自动获取最近 20 条分析记录，动态计算坐标轴范围，并绘制出信噪比 (SNR) 的变化曲线，帮助用户快速识别设备状态或样本稳定性的波动。
*   **批量数据清理**:
    *   **背景**: 在实验调试过程中会产生大量无效（BAD）数据，逐条删除效率极低。
    *   **实现**: 引入了批量清理功能。通过顶部栏的“清理”按钮，用户可以一键筛选并删除所有质量评级为“BAD”的记录。为了防止误操作，我们还封装了标准的 Material Design 确认对话框 (`AlertDialog`)。
